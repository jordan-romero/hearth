packages:
  - "apps/*"
  - "packages/*"

# Native Opus for voice capture. pnpm 10 skips dependency install scripts unless listed; this one
# fetches a prebuilt binary for the platform. If it can't, the bot falls back to WebAssembly Opus.
onlyBuiltDependencies:
  - "@discordjs/opus"
